export const environment = {
  production: false,
  // URL base del backend en desarrollo (host local). Si prefieres usar el proxy en dev
  // cambia este valor a '/api' y asegúrate de reiniciar el dev server.
  urlBackEnd: 'https://as241s3-t06-be-2.onrender.com',
  // URL separada para la API de Pedidos (orders). Apunta al controlador de ventas del backend.
  urlPedidosBackEnd: 'https://as241s3-t06-be-2.onrender.com/api/v1/sale'
  };
